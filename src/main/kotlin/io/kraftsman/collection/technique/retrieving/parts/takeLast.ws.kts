package io.kraftsman.collection.technique.retrieving.parts

/**
 * takeLast() - 從集合後面取 n 個值
 */

// 建立範例資料
val numbers = listOf(1, 2, 3, 4, 5, 6)

// 測試 APIs
numbers.takeLast(3)
numbers.takeLast(100)
