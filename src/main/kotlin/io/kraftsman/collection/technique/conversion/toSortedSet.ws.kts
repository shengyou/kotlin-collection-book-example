package io.kraftsman.collection.technique.conversion

/**
 * toSortedSet() - 依 Natural Ordering 排序後轉成 SortedSet
 */

// 建立範例資料
val numberList = listOf(10, 10, 3, 3, 2, 2, 2, 4, 7, 5)

// 測試 APIs
numberList.toSortedSet()
