package io.kraftsman.collection.technique.creation

/**
 * arrayOf() - 建立 Array
 */

// 建立範例資料
arrayOf(1, 2, 3, 4, 5)
arrayOf(1, 2, 3, 4, 5).contentToString()

arrayOf<Int>()
arrayOf<Int>().contentToString()
