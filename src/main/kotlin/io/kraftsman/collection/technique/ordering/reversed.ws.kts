package io.kraftsman.collection.technique.ordering

/**
 * reversed() - 反轉 Collection 內元素的排列順序
 */

// 建立範例資料
val numbers = listOf(1, 5, 3, 2, 4)

// 測試 APIs
numbers.reversed()
numbers.sorted().reversed()
