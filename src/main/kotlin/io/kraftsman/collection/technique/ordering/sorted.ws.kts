package io.kraftsman.collection.technique.ordering

/**
 * sorted() - 依 Natural Order 正向排序
 */

// 建立範例資料
val numbers = listOf(1, 2, 3, 4, 5)
val string = setOf("one", "two", "three", "four", "five")

// 測試 APIs
numbers.sorted()
string.sorted()
