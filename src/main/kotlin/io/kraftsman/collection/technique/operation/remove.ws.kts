package io.kraftsman.collection.technique.operation

/**
 * remove() - 在 Collection 裡搜尋指定元素並刪除
 */

// 建立範例資料
val numbers = mutableListOf(1, 3, 5, 6, 7)

// 測試 APIs
numbers.remove(3)
numbers.remove(100)
